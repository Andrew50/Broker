apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-deployment
  labels:
    app: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: billin19/backend:latest
          ports:
            - containerPort: 80
